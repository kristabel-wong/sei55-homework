<h2>All the hits</h2>
<%# Loop through and list the hits %>
<table>
  <% @hits.each do |hit|%>
    <tr>
      <td>(<%= hit.year_released %>)</td>
      <td><a href="/hits/<%= hit.id %>"><%= hit.title %></a></td>
    </tr>
  <% end %>
</table>
