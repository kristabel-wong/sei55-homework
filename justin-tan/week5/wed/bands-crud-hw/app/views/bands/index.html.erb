<h2>Bands Compendium</h2>

<% @bands.each do |band| %>

<%= link_to image_tag(band.image, alt: band.name, class: 'band'), band_path(band.id) %>
<!--
<%= image_tag band.image, alt: band.name, class: 'band' %>
-->
<p><%= band.name %></p>

<% end %>
